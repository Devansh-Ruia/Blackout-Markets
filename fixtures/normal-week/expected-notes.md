# Normal Week

Balanced prices, moderate carbon, and enough capacity for ordinary moves.

This dataset demonstrates:

- moderate estimated savings from moving training work out of `us-east-1`
- a latency-sensitive critical inference job that stays in place or requires care
- a data residency pinned workload in `eu-west-1`
- no built-in UI loading; upload these files manually
